---
import Layout from '../../../../layouts/Base.astro';
import Repl from '../../../../components/Repl';
import SchemeRepl from '../../../../components/SchemeRepl';
import PrevNext from "../../../../components/PrevNext.astro";
---

<Layout title="Number Theory Ch.8 — Continued Fractions" favicon="#">
  <main>

    <a href="/reading/number-theory/" class="text-sm mb-4 inline-block">&larr; back to Number Theory</a>

    <h1>Continued Fractions</h1>
    <p class="text-gray-400 text-sm mb-6">Jim Hefferon &middot; Elementary Number Theory &middot; <a href="https://jheffero.w3.uvm.edu/numbertheory/">Ch. 8</a></p>

    <div class="bg-zinc-800 rounded-lg p-5 mb-8 callout">
      <p class="text-lg" style="max-width:none">A <strong>continued fraction</strong> writes a number as a chain of integer parts and reciprocals. The convergents (partial evaluations) are the best rational approximations. Quadratic irrationals like sqrt(2) have periodic continued fractions, and this connects to Pell's equation.</p>
    </div>

    <!-- SVG: Convergents approaching sqrt(2) on a number line -->
    <h3>Convergents as best approximations</h3>
    <p>sqrt(2) = [1; 2, 2, 2, ...]. The convergents 1/1, 3/2, 7/5, 17/12, 41/29, ... alternate above and below sqrt(2), getting closer each time. No fraction with a smaller denominator gets closer.</p>

    <div class="flex justify-center my-6">
      <svg viewBox="0 0 440 110" class="w-full max-w-lg" style="font-family: ui-monospace, monospace;">
        <text x="220" y="15" text-anchor="middle" fill="currentColor" font-size="10" fill-opacity="0.35">Convergents of sqrt(2) on the number line</text>
        <!-- Number line segment from 1.0 to 1.5 -->
        <line x1="20" y1="60" x2="420" y2="60" stroke="currentColor" stroke-width="1" stroke-opacity="0.3"/>
        <!-- Scale: 1.0 at x=20, 1.5 at x=420. 1 unit = 800px -->
        <!-- sqrt(2) ~ 1.4142 at x = 20 + (1.4142-1.0)*800 = 20+331 = 351 -->
        <line x1="351" y1="30" x2="351" y2="90" stroke="#facc15" stroke-width="1.5" stroke-dasharray="3,2"/>
        <text x="351" y="100" text-anchor="middle" fill="#facc15" font-size="9">sqrt(2)</text>
        <!-- Tick marks -->
        <line x1="20" y1="55" x2="20" y2="65" stroke="currentColor" stroke-width="1" stroke-opacity="0.3"/>
        <text x="20" y="78" text-anchor="middle" fill="currentColor" font-size="8" fill-opacity="0.3">1.0</text>
        <line x1="220" y1="55" x2="220" y2="65" stroke="currentColor" stroke-width="1" stroke-opacity="0.3"/>
        <text x="220" y="78" text-anchor="middle" fill="currentColor" font-size="8" fill-opacity="0.3">1.25</text>
        <line x1="420" y1="55" x2="420" y2="65" stroke="currentColor" stroke-width="1" stroke-opacity="0.3"/>
        <text x="420" y="78" text-anchor="middle" fill="currentColor" font-size="8" fill-opacity="0.3">1.5</text>
        <!-- 1/1 = 1.0 at x=20 -->
        <circle cx="20" cy="60" r="4" fill="#ef4444" fill-opacity="0.6"/>
        <text x="20" y="28" text-anchor="middle" fill="#ef4444" font-size="9">1/1</text>
        <!-- 3/2 = 1.5 at x=420 -->
        <circle cx="420" cy="60" r="4" fill="#60a5fa" fill-opacity="0.6"/>
        <text x="420" y="28" text-anchor="middle" fill="#60a5fa" font-size="9">3/2</text>
        <!-- 7/5 = 1.4 at x=20+(0.4*800)=340 -->
        <circle cx="340" cy="60" r="4" fill="#ef4444" fill-opacity="0.6"/>
        <text x="340" y="28" text-anchor="middle" fill="#ef4444" font-size="9">7/5</text>
        <!-- 17/12 = 1.4167 at x=20+(0.4167*800)=353 -->
        <circle cx="353" cy="60" r="3" fill="#60a5fa" fill-opacity="0.6"/>
        <text x="370" y="40" text-anchor="middle" fill="#60a5fa" font-size="8">17/12</text>
        <!-- 41/29 = 1.41379 at x=20+(0.41379*800)=351.03 -->
        <circle cx="351" cy="55" r="2" fill="#ef4444" fill-opacity="0.6"/>
        <text x="330" y="45" text-anchor="middle" fill="#ef4444" font-size="8">41/29</text>
      </svg>
    </div>

    <SchemeRepl client:visible initialCode={`; Continued fraction expansion of a rational number
(define (cf-rational p q)
  (if (= q 0) (list)
      (cons (quotient p q)
            (cf-rational q (modulo p q)))))

(display "CF of 355/113 = ") (display (cf-rational 355 113)) (newline)
; [3; 7, 16, 11] -- wait, this is actually [3; 7, 16]
; 355/113 is the famous approximation to pi

(display "CF of 17/12  = ") (display (cf-rational 17 12)) (newline)
; [1; 2, 2, 2] -- convergent of sqrt(2)

(display "CF of 41/29  = ") (display (cf-rational 41 29)) (newline)
; [1; 2, 2, 2, 2] -- next convergent of sqrt(2)

; Evaluate a continued fraction [a0; a1, a2, ...]
(define (cf-eval coeffs)
  (if (null? (cdr coeffs))
      (list (car coeffs) 1)  ; p/q as (p q)
      (let* ((rest (cf-eval (cdr coeffs)))
             (p (car rest))
             (q (cadr rest)))
        (list (+ (* (car coeffs) p) q) p))))

(let ((r (cf-eval (list 1 2 2 2 2 2))))
  (display "CF [1;2,2,2,2,2] = ")
  (display (car r)) (display "/") (display (cadr r)))
`} />

    <div class="python-block my-2" style="display:none">
      <Repl client:visible initialCode={`# Continued fractions
def cf_rational(p, q):
    """Continued fraction expansion of p/q."""
    coeffs = []
    while q != 0:
        coeffs.append(p // q)
        p, q = q, p % q
    return coeffs

def convergents(coeffs):
    """Compute convergents from CF coefficients."""
    p_prev, p_curr = 0, 1
    q_prev, q_curr = 1, 0
    result = []
    for a in coeffs:
        p_prev, p_curr = p_curr, a * p_curr + p_prev
        q_prev, q_curr = q_curr, a * q_curr + q_prev
        result.append((p_curr, q_curr))
    return result

print(f"CF of 355/113 = {cf_rational(355, 113)}")

# sqrt(2) = [1; 2, 2, 2, ...]
cf_sqrt2 = [1] + [2] * 10
convs = convergents(cf_sqrt2)
print("Convergents of sqrt(2):")
for p, q in convs[:6]:
    print(f"  {p}/{q} = {p/q:.10f}")
print(f"  sqrt(2)   = {2**0.5:.10f}")
`} />
    </div>


    <!-- Pell's equation -->
    <h3>Pell's equation</h3>
    <p>The equation x^2 - D y^2 = 1 (for non-square D) has infinitely many solutions. The fundamental solution comes from the convergents of the continued fraction of sqrt(D). For sqrt(2) = [1; 2, 2, ...], the convergent 3/2 gives 3^2 - 2(2^2) = 9 - 8 = 1.</p>

    <SchemeRepl client:visible initialCode={`; Pell's equation: x^2 - D*y^2 = 1
; Find solutions from convergents of sqrt(D)

; Convergents of [1; 2, 2, 2, ...]  (sqrt(2))
(define (convergents-sqrt2 n)
  (let loop ((k 0) (p-prev 0) (p-curr 1) (q-prev 1) (q-curr 0) (results (list)))
    (if (>= k n)
        (reverse results)
        (let* ((a (if (= k 0) 1 2))
               (p-next (+ (* a p-curr) p-prev))
               (q-next (+ (* a q-curr) q-prev)))
          (loop (+ k 1) p-curr p-next q-curr q-next
                (cons (list p-next q-next) results))))))

(display "Convergents and Pell check (x^2 - 2y^2):") (newline)
(for-each
  (lambda (pair)
    (let ((x (car pair)) (y (cadr pair)))
      (display "  ") (display x) (display "/") (display y)
      (display "  ->  ") (display x) (display "^2 - 2*")
      (display y) (display "^2 = ")
      (display (- (* x x) (* 2 y y))) (newline)))
  (convergents-sqrt2 8))
; Solutions to Pell: where the value is 1 or -1
`} />


    <PrevNext prev={{ href: "/reading/number-theory/numthy-07/", label: "Primitive Roots" }} />

  </main>
</Layout>
