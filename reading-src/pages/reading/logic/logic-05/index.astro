---
import Layout from '../../../../layouts/Base.astro';
import Repl from '../../../../components/Repl';
import SchemeRepl from '../../../../components/SchemeRepl';
import Neighbors from "../../../../components/Neighbors.astro";
import PrevNext from "../../../../components/PrevNext.astro";
---

<Layout title="Logic Ch.5 — Natural Deduction" favicon="🔑">
  <main>

    <a href="/reading/logic/" class="text-sm mb-4 inline-block">&larr; back to logic</a>

    <h1>Natural Deduction</h1>
    <p class="text-gray-400 text-sm mb-6">Craig DeLancey &middot; A Concise Introduction to Logic, Ch. 5 &middot; <a href="https://milneopentextbooks.org/a-concise-introduction-to-logic/">CC BY-SA 4.0</a></p>

    <div class="bg-zinc-800 rounded-lg p-5 mb-8 callout">
      <p class="text-lg" style="max-width:none"><strong>Natural deduction</strong> proves conclusions from premises using inference rules. Each rule introduces or eliminates a connective. Subproofs (temporary assumptions) let you prove conditionals and handle disjunctions.</p>
    </div>

    <!-- SVG: Fitch-style proof with subproof -->
    <div class="flex justify-center my-6">
      <svg viewBox="0 0 340 240" class="w-full max-w-sm" style="font-family: ui-monospace, monospace;">
        <!-- Main proof scope line -->
        <line x1="30" y1="15" x2="30" y2="220" stroke="currentColor" stroke-width="1.5" stroke-opacity="0.3"/>
        <!-- Premise -->
        <text x="40" y="30" fill="currentColor" font-size="12" fill-opacity="0.7">1. P &rarr; Q</text>
        <text x="260" y="30" fill="currentColor" font-size="10" fill-opacity="0.35">premise</text>
        <text x="40" y="52" fill="currentColor" font-size="12" fill-opacity="0.7">2. Q &rarr; R</text>
        <text x="260" y="52" fill="currentColor" font-size="10" fill-opacity="0.35">premise</text>
        <line x1="30" y1="60" x2="310" y2="60" stroke="#71717a" stroke-width="1" stroke-opacity="0.45"/>
        <!-- Subproof scope line -->
        <line x1="50" y1="68" x2="50" y2="160" stroke="#60a5fa" stroke-width="1.5" stroke-opacity="0.4"/>
        <!-- Assumption -->
        <text x="60" y="82" fill="#60a5fa" font-size="12">3. P</text>
        <text x="260" y="82" fill="currentColor" font-size="10" fill-opacity="0.35">assume</text>
        <line x1="50" y1="90" x2="240" y2="90" stroke="#60a5fa" stroke-width="1" stroke-opacity="0.2"/>
        <!-- Steps inside subproof -->
        <text x="60" y="110" fill="currentColor" font-size="12" fill-opacity="0.7">4. Q</text>
        <text x="260" y="110" fill="currentColor" font-size="10" fill-opacity="0.35">&rarr;E 1,3</text>
        <text x="60" y="132" fill="currentColor" font-size="12" fill-opacity="0.7">5. R</text>
        <text x="260" y="132" fill="currentColor" font-size="10" fill-opacity="0.35">&rarr;E 2,4</text>
        <!-- End subproof, conclude conditional -->
        <text x="40" y="180" fill="#f59e0b" font-size="12">6. P &rarr; R</text>
        <text x="260" y="180" fill="currentColor" font-size="10" fill-opacity="0.35">&rarr;I 3-5</text>
        <!-- Caption -->
        <text x="170" y="225" text-anchor="middle" fill="currentColor" font-size="10" fill-opacity="0.35">Hypothetical syllogism proved by conditional proof</text>
      </svg>
    </div>

    <h3>Modus ponens and modus tollens</h3>
    <p><strong>Modus ponens</strong> (&rarr;E): from P and P &rarr; Q, conclude Q. <strong>Modus tollens</strong>: from &not;Q and P &rarr; Q, conclude &not;P.</p>

    <SchemeRepl client:visible initialCode={`; Modus ponens: if P is true and P -> Q, then Q
(define (modus-ponens p p-implies-q)
  (if p (p-implies-q p) "cannot apply: P is false"))

; Modus tollens: if not Q and P -> Q, then not P
(define (modus-tollens not-q p-implies-q)
  ; If Q is false and P -> Q, then P must be false
  (if not-q "not P" "cannot apply: Q is true"))

; Example: P = "it rains", Q = "ground is wet"
(define (rain-implies-wet p) "ground is wet")

(display "Modus ponens: it rains, rain->wet: ")
(display (modus-ponens #t rain-implies-wet)) (newline)

(display "Modus tollens: ground not wet, rain->wet: ")
(display (modus-tollens #t rain-implies-wet))
`} />

    <div class="python-block my-2" style="display:none">
      <Repl client:visible initialCode={`# Proof rules as functions
def modus_ponens(p, p_implies_q):
    """From P and P->Q, derive Q"""
    assert p, "Cannot apply MP: P is false"
    return p_implies_q(p)

def modus_tollens(not_q, p_implies_q_holds):
    """From not-Q and P->Q, derive not-P"""
    assert not_q, "Cannot apply MT: Q is true"
    return True  # not P

# Example
rain_implies_wet = lambda p: True  # Q = wet
result = modus_ponens(True, rain_implies_wet)
print(f"Modus ponens: rain + (rain->wet) = wet: {result}")
print(f"Modus tollens: not wet + (rain->wet) = not rain: {modus_tollens(True, True)}")
`} />
    </div>

    <h3>Conjunction and disjunction rules</h3>
    <p>Conjunction introduction (&and;I): from P and Q separately, conclude P &and; Q. Conjunction elimination (&and;E): from P &and; Q, conclude P (or Q). Disjunction introduction (&or;I): from P, conclude P &or; Q for any Q. Disjunction elimination (&or;E): from P &or; Q, if P leads to R and Q leads to R, conclude R.</p>

    <SchemeRepl client:visible initialCode={`; Conjunction intro: from P and Q, derive P and Q
(define (conj-intro p q) (cons p q))

; Conjunction elim: from P and Q, derive P (or Q)
(define (conj-elim-left pq) (car pq))
(define (conj-elim-right pq) (cdr pq))

; Disjunction intro: from P, derive P or Q
(define (disj-intro-left p) (list (quote or) p (quote anything)))

; Disjunction elim: from P or Q, P->R, Q->R, derive R
(define (disj-elim p-or-q p-gives-r q-gives-r)
  (if (car p-or-q)
    (p-gives-r (car p-or-q))
    (q-gives-r (cdr p-or-q))))

; Demo
(define pq (conj-intro "it rains" "it is cold"))
(display "Conj intro: ") (display pq) (newline)
(display "Conj elim left: ") (display (conj-elim-left pq)) (newline)
(display "Conj elim right: ") (display (conj-elim-right pq))
`} />

    <h3>Conditional proof and RAA</h3>
    <p><strong>Conditional proof</strong> (&rarr;I): assume P, derive Q inside a subproof, then discharge the assumption and conclude P &rarr; Q. <strong>Reductio ad absurdum</strong> (RAA): assume P, derive a contradiction, conclude &not;P.</p>

    <SchemeRepl client:visible initialCode={`; Conditional proof: assume P, derive Q, conclude P -> Q
; In code: a conditional proof IS a lambda

; Prove: P -> (P or Q)
; Assume P, apply disjunction intro, get P or Q
(define proof-1
  (lambda (p) (or p #f)))  ; P -> (P or Q)

(display "P -> (P or Q) with P=T: ")
(display (proof-1 #t)) (newline)
(display "P -> (P or Q) with P=F: ")
(display (proof-1 #f)) (newline)

; RAA: assume P, derive contradiction, conclude not P
; Prove: not (P and not P)
; Assume (P and not P), that is itself a contradiction
(define (raa assumption)
  ; If assumption leads to contradiction, negate it
  (not assumption))

(display "RAA on (T and not T): ")
(display (raa (and #t (not #t)))) (newline)
(display "Conclusion: not (P and not P) = ")
(display (not (and #t (not #t))))
`} />

    <h3>Inference rules reference</h3>
    <div class="overflow-x-auto">
      <table class="w-full text-sm">
        <thead>
          <tr class="border-b border-zinc-700">
            <th class="py-2 pr-4">Rule</th>
            <th class="py-2 pr-4">From</th>
            <th class="py-2">Conclude</th>
          </tr>
        </thead>
        <tbody>
          <tr class="border-b border-zinc-800"><td class="py-2 pr-4">Modus ponens (&rarr;E)</td><td class="pr-4 font-mono">P, P &rarr; Q</td><td class="font-mono">Q</td></tr>
          <tr class="border-b border-zinc-800"><td class="py-2 pr-4">Modus tollens</td><td class="pr-4 font-mono">&not;Q, P &rarr; Q</td><td class="font-mono">&not;P</td></tr>
          <tr class="border-b border-zinc-800"><td class="py-2 pr-4">Conj intro (&and;I)</td><td class="pr-4 font-mono">P, Q</td><td class="font-mono">P &and; Q</td></tr>
          <tr class="border-b border-zinc-800"><td class="py-2 pr-4">Conj elim (&and;E)</td><td class="pr-4 font-mono">P &and; Q</td><td class="font-mono">P (or Q)</td></tr>
          <tr class="border-b border-zinc-800"><td class="py-2 pr-4">Disj intro (&or;I)</td><td class="pr-4 font-mono">P</td><td class="font-mono">P &or; Q</td></tr>
          <tr class="border-b border-zinc-800"><td class="py-2 pr-4">Disj elim (&or;E)</td><td class="pr-4 font-mono">P &or; Q, P&rarr;R, Q&rarr;R</td><td class="font-mono">R</td></tr>
          <tr class="border-b border-zinc-800"><td class="py-2 pr-4">Cond proof (&rarr;I)</td><td class="pr-4 font-mono">[assume P ... Q]</td><td class="font-mono">P &rarr; Q</td></tr>
          <tr><td class="py-2 pr-4">RAA</td><td class="pr-4 font-mono">[assume P ... &perp;]</td><td class="font-mono">&not;P</td></tr>
        </tbody>
      </table>
    </div>

    <Neighbors>
    <ul class="neighbors">
      <li><a href="/reading/natural-breadcrumbs/staton-2025/">🍞 Staton 2025</a> &mdash; Hoare logic rules are proof rules for programs</li>
    
      <li><a href="/reading/proof-writing/proofs-02/">✎ Proofs Ch.2</a> &mdash; direct proof is natural deduction without the formalism</li>
      <li><a href="/reading/the-natural-framework-lean/hoare-core/">✏️ Hoare/Core.lean</a> &mdash; Hoare triples use sequent-style reasoning</li>
      <li><a href="/reading/category-theory/milewski-01/">🐱 Category Theory Ch.1</a> &mdash; composition laws mirror the sequencing rules of natural deduction</li>
    </ul>
    </Neighbors>


    <PrevNext prev={{ href: "/reading/logic/logic-04/", label: "Logical Equivalence" }} next={{ href: "/reading/logic/logic-06/", label: "Predicate Logic" }} />

  </main>
</Layout>
